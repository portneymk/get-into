# Optional. Falls back to content/_config.json (claypool).
NEXT_PUBLIC_DEFAULT_PACK=claypool

# Optional. The site ships without this — why-next is checked-in / fallback text.
# OPENAI_API_KEY=
# WHY_NEXT_API_KEY=
